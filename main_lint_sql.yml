
- name: lint sql
  expect:
    chdir: "{{ code_dir }}/{{ project }}"
    command: "{{ virtualenv_dir }}/bin/sqlfluff fix ./{{ input_file }} --dialect mysql"
    responses:
      'Are you sure you wish to attempt to fix these': "\n"
  become: false
  ignore_errors: true
